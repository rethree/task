// import { test } from "tap";

// test("if no computation fails, queue is exhausted", t => {

// });

// test("failing computation triggers early completion", t => {

// });

// test("failing computation carries fault", t => {

// });
